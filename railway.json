{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --ignore-scripts=false && npm run build --workspace=@tradeagent/hedera && npm run build --workspace=@tradeagent/shared && cd apps/api && npx prisma generate && npm run build && cd /app && npm prune --omit=dev"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
