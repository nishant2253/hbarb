{
  "name": "@tradeagent/hedera",
  "version": "1.0.0",
  "description": "TradeAgent — Hedera integration layer (HCS, HTS, HFS, HCS-10)",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --skipLibCheck --noEmitOnError false",
    "build:watch": "tsc --project tsconfig.json --skipLibCheck --noEmitOnError false --watch"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.51.0",
    "@hashgraphonline/standards-agent-kit": "^0.2.166",
    "@hashgraphonline/standards-sdk": "^0.1.168",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
